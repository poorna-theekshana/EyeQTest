<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.eyeqtest">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EyeQTest"
        tools:targetApi="31">
        <activity
            android:name=".CloseRightEye"
            android:exported="false" />
        <activity
            android:name=".CloseLeftEye"
            android:exported="false" />
        <activity
            android:name=".AstigmatismFront"
            android:exported="false" />
        <activity
            android:name=".Astigmatism"
            android:exported="false" />
        <activity
            android:name=".ContrastSensivityActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ResultActivity">

            <!-- Add any intent filters or other configurations as needed -->
        </activity>
    </application>

</manifest>